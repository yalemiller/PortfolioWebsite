{
  "devDependencies": {
    "sharp": "^0.35.4"
  },
  "name": "yalemiller-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Static portfolio site for yalemiller.com",
  "scripts": {
    "build": "node build.js",
    "images": "node scripts/images.js",
    "serve": "python -m http.server 8765 --bind 127.0.0.1"
  }
}
